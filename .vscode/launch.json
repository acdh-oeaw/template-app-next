{
	"version": "0.2.0",
	"configurations": [
		{
			"type": "bun",
			"request": "launch",
			"name": "Debug script",
			"program": "${file}",
			"cwd": "${workspaceFolder}",
			"stopOnEntry": false
		}
	]
}
